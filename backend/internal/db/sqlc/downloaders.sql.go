// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: downloaders.sql

package dbgen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createDownloader = `-- name: CreateDownloader :one
insert into downloader (name, type, protocol, url, username, password, config_json, enabled, "default")
values ($1, $2, $3, $4, $5, $6, $7, $8, $9)
returning id, name, type, protocol, url, username, password, config_json, enabled, "default", created_at, updated_at
`

type CreateDownloaderParams struct {
	Name           string  `json:"name"`
	DownloaderType string  `json:"downloader_type"`
	Protocol       string  `json:"protocol"`
	Url            string  `json:"url"`
	Username       *string `json:"username"`
	Password       *string `json:"password"`
	ConfigJson     []byte  `json:"config_json"`
	Enabled        bool    `json:"enabled"`
	IsDefault      bool    `json:"is_default"`
}

func (q *Queries) CreateDownloader(ctx context.Context, arg CreateDownloaderParams) (Downloader, error) {
	row := q.db.QueryRow(ctx, createDownloader,
		arg.Name,
		arg.DownloaderType,
		arg.Protocol,
		arg.Url,
		arg.Username,
		arg.Password,
		arg.ConfigJson,
		arg.Enabled,
		arg.IsDefault,
	)
	var i Downloader
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Type,
		&i.Protocol,
		&i.Url,
		&i.Username,
		&i.Password,
		&i.ConfigJson,
		&i.Enabled,
		&i.Default,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteDownloader = `-- name: DeleteDownloader :exec
delete from downloader where id = $1
`

func (q *Queries) DeleteDownloader(ctx context.Context, id pgtype.UUID) error {
	_, err := q.db.Exec(ctx, deleteDownloader, id)
	return err
}

const getDefaultDownloader = `-- name: GetDefaultDownloader :one
select id, name, type, protocol, url, username, password, config_json, enabled, "default", created_at, updated_at from downloader
where protocol = $1 and "default" = true
`

func (q *Queries) GetDefaultDownloader(ctx context.Context, protocol string) (Downloader, error) {
	row := q.db.QueryRow(ctx, getDefaultDownloader, protocol)
	var i Downloader
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Type,
		&i.Protocol,
		&i.Url,
		&i.Username,
		&i.Password,
		&i.ConfigJson,
		&i.Enabled,
		&i.Default,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getDownloader = `-- name: GetDownloader :one
select id, name, type, protocol, url, username, password, config_json, enabled, "default", created_at, updated_at from downloader
where id = $1
`

func (q *Queries) GetDownloader(ctx context.Context, id pgtype.UUID) (Downloader, error) {
	row := q.db.QueryRow(ctx, getDownloader, id)
	var i Downloader
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Type,
		&i.Protocol,
		&i.Url,
		&i.Username,
		&i.Password,
		&i.ConfigJson,
		&i.Enabled,
		&i.Default,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listDownloaders = `-- name: ListDownloaders :many

select id, name, type, protocol, url, username, password, config_json, enabled, "default", created_at, updated_at from downloader
order by name asc
`

// Downloaders
func (q *Queries) ListDownloaders(ctx context.Context) ([]Downloader, error) {
	rows, err := q.db.Query(ctx, listDownloaders)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Downloader
	for rows.Next() {
		var i Downloader
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Type,
			&i.Protocol,
			&i.Url,
			&i.Username,
			&i.Password,
			&i.ConfigJson,
			&i.Enabled,
			&i.Default,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateDownloader = `-- name: UpdateDownloader :one
update downloader
set name = $1,
    type = $2,
    protocol = $3,
    url = $4,
    username = $5,
    password = $6,
    config_json = $7,
    enabled = $8,
    "default" = $9,
    updated_at = now()
where id = $10
returning id, name, type, protocol, url, username, password, config_json, enabled, "default", created_at, updated_at
`

type UpdateDownloaderParams struct {
	Name           string      `json:"name"`
	DownloaderType string      `json:"downloader_type"`
	Protocol       string      `json:"protocol"`
	Url            string      `json:"url"`
	Username       *string     `json:"username"`
	Password       *string     `json:"password"`
	ConfigJson     []byte      `json:"config_json"`
	Enabled        bool        `json:"enabled"`
	IsDefault      bool        `json:"is_default"`
	ID             pgtype.UUID `json:"id"`
}

func (q *Queries) UpdateDownloader(ctx context.Context, arg UpdateDownloaderParams) (Downloader, error) {
	row := q.db.QueryRow(ctx, updateDownloader,
		arg.Name,
		arg.DownloaderType,
		arg.Protocol,
		arg.Url,
		arg.Username,
		arg.Password,
		arg.ConfigJson,
		arg.Enabled,
		arg.IsDefault,
		arg.ID,
	)
	var i Downloader
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Type,
		&i.Protocol,
		&i.Url,
		&i.Username,
		&i.Password,
		&i.ConfigJson,
		&i.Enabled,
		&i.Default,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
