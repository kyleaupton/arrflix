// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: tracking.sql

package dbgen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createTracking = `-- name: CreateTracking :one

INSERT INTO tracking (media_item_id, quality_profile_id, state, scope, upgrade_behavior, schedule_strategy, autonomy_backfill, autonomy_ongoing)
VALUES (
  $1,
  $2,
  $3,
  $4,
  $5,
  $6,
  $7,
  $8
)
RETURNING id, media_item_id, quality_profile_id, state, scope, upgrade_behavior, schedule_strategy, autonomy_backfill, autonomy_ongoing, created_at, updated_at
`

type CreateTrackingParams struct {
	MediaItemID      pgtype.UUID `json:"media_item_id"`
	QualityProfileID pgtype.UUID `json:"quality_profile_id"`
	State            string      `json:"state"`
	Scope            string      `json:"scope"`
	UpgradeBehavior  string      `json:"upgrade_behavior"`
	ScheduleStrategy string      `json:"schedule_strategy"`
	AutonomyBackfill string      `json:"autonomy_backfill"`
	AutonomyOngoing  string      `json:"autonomy_ongoing"`
}

// Tracking: the ongoing-intent primitive.
func (q *Queries) CreateTracking(ctx context.Context, arg CreateTrackingParams) (Tracking, error) {
	row := q.db.QueryRow(ctx, createTracking,
		arg.MediaItemID,
		arg.QualityProfileID,
		arg.State,
		arg.Scope,
		arg.UpgradeBehavior,
		arg.ScheduleStrategy,
		arg.AutonomyBackfill,
		arg.AutonomyOngoing,
	)
	var i Tracking
	err := row.Scan(
		&i.ID,
		&i.MediaItemID,
		&i.QualityProfileID,
		&i.State,
		&i.Scope,
		&i.UpgradeBehavior,
		&i.ScheduleStrategy,
		&i.AutonomyBackfill,
		&i.AutonomyOngoing,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const createTrackingIfAbsent = `-- name: CreateTrackingIfAbsent :one
INSERT INTO tracking (media_item_id, quality_profile_id, state, scope, upgrade_behavior, schedule_strategy, autonomy_backfill, autonomy_ongoing)
VALUES (
  $1,
  $2,
  $3,
  $4,
  $5,
  $6,
  $7,
  $8
)
ON CONFLICT (media_item_id) DO NOTHING
RETURNING id, media_item_id, quality_profile_id, state, scope, upgrade_behavior, schedule_strategy, autonomy_backfill, autonomy_ongoing, created_at, updated_at
`

type CreateTrackingIfAbsentParams struct {
	MediaItemID      pgtype.UUID `json:"media_item_id"`
	QualityProfileID pgtype.UUID `json:"quality_profile_id"`
	State            string      `json:"state"`
	Scope            string      `json:"scope"`
	UpgradeBehavior  string      `json:"upgrade_behavior"`
	ScheduleStrategy string      `json:"schedule_strategy"`
	AutonomyBackfill string      `json:"autonomy_backfill"`
	AutonomyOngoing  string      `json:"autonomy_ongoing"`
}

// CreateTrackingIfAbsent is the race-safe get-or-create for the dedup boundary:
// ON CONFLICT (media_item_id) DO NOTHING means a concurrent spawn that lost the
// insert matches 0 rows (surfaced as pgx.ErrNoRows) instead of erroring on the
// UNIQUE violation. The caller reads no-row as "already tracked, re-read it".
func (q *Queries) CreateTrackingIfAbsent(ctx context.Context, arg CreateTrackingIfAbsentParams) (Tracking, error) {
	row := q.db.QueryRow(ctx, createTrackingIfAbsent,
		arg.MediaItemID,
		arg.QualityProfileID,
		arg.State,
		arg.Scope,
		arg.UpgradeBehavior,
		arg.ScheduleStrategy,
		arg.AutonomyBackfill,
		arg.AutonomyOngoing,
	)
	var i Tracking
	err := row.Scan(
		&i.ID,
		&i.MediaItemID,
		&i.QualityProfileID,
		&i.State,
		&i.Scope,
		&i.UpgradeBehavior,
		&i.ScheduleStrategy,
		&i.AutonomyBackfill,
		&i.AutonomyOngoing,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const findTrackingByMediaItem = `-- name: FindTrackingByMediaItem :one
SELECT id, media_item_id, quality_profile_id, state, scope, upgrade_behavior, schedule_strategy, autonomy_backfill, autonomy_ongoing, created_at, updated_at FROM tracking
WHERE media_item_id = $1
`

// FindTrackingByMediaItem is the dedup lookup: a movie request resolves to its
// existing tracking (if any) before deciding to create a new one. No-row is a
// normal "not yet tracked" signal, surfaced as NotFound via FromPg.
func (q *Queries) FindTrackingByMediaItem(ctx context.Context, mediaItemID pgtype.UUID) (Tracking, error) {
	row := q.db.QueryRow(ctx, findTrackingByMediaItem, mediaItemID)
	var i Tracking
	err := row.Scan(
		&i.ID,
		&i.MediaItemID,
		&i.QualityProfileID,
		&i.State,
		&i.Scope,
		&i.UpgradeBehavior,
		&i.ScheduleStrategy,
		&i.AutonomyBackfill,
		&i.AutonomyOngoing,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getTracking = `-- name: GetTracking :one
SELECT id, media_item_id, quality_profile_id, state, scope, upgrade_behavior, schedule_strategy, autonomy_backfill, autonomy_ongoing, created_at, updated_at FROM tracking
WHERE id = $1
`

func (q *Queries) GetTracking(ctx context.Context, id pgtype.UUID) (Tracking, error) {
	row := q.db.QueryRow(ctx, getTracking, id)
	var i Tracking
	err := row.Scan(
		&i.ID,
		&i.MediaItemID,
		&i.QualityProfileID,
		&i.State,
		&i.Scope,
		&i.UpgradeBehavior,
		&i.ScheduleStrategy,
		&i.AutonomyBackfill,
		&i.AutonomyOngoing,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listTrackings = `-- name: ListTrackings :many
SELECT id, media_item_id, quality_profile_id, state, scope, upgrade_behavior, schedule_strategy, autonomy_backfill, autonomy_ongoing, created_at, updated_at FROM tracking
ORDER BY created_at DESC
`

func (q *Queries) ListTrackings(ctx context.Context) ([]Tracking, error) {
	rows, err := q.db.Query(ctx, listTrackings)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Tracking
	for rows.Next() {
		var i Tracking
		if err := rows.Scan(
			&i.ID,
			&i.MediaItemID,
			&i.QualityProfileID,
			&i.State,
			&i.Scope,
			&i.UpgradeBehavior,
			&i.ScheduleStrategy,
			&i.AutonomyBackfill,
			&i.AutonomyOngoing,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const setTrackingAutonomy = `-- name: SetTrackingAutonomy :one
UPDATE tracking
SET autonomy_backfill = $1,
    autonomy_ongoing = $2,
    updated_at = now()
WHERE id = $3
RETURNING id, media_item_id, quality_profile_id, state, scope, upgrade_behavior, schedule_strategy, autonomy_backfill, autonomy_ongoing, created_at, updated_at
`

type SetTrackingAutonomyParams struct {
	AutonomyBackfill string      `json:"autonomy_backfill"`
	AutonomyOngoing  string      `json:"autonomy_ongoing"`
	ID               pgtype.UUID `json:"id"`
}

// SetTrackingAutonomy sets both per-segment autonomy dials. The service holds
// the hold/release of affected wants in the same transaction.
func (q *Queries) SetTrackingAutonomy(ctx context.Context, arg SetTrackingAutonomyParams) (Tracking, error) {
	row := q.db.QueryRow(ctx, setTrackingAutonomy, arg.AutonomyBackfill, arg.AutonomyOngoing, arg.ID)
	var i Tracking
	err := row.Scan(
		&i.ID,
		&i.MediaItemID,
		&i.QualityProfileID,
		&i.State,
		&i.Scope,
		&i.UpgradeBehavior,
		&i.ScheduleStrategy,
		&i.AutonomyBackfill,
		&i.AutonomyOngoing,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const setTrackingState = `-- name: SetTrackingState :one
UPDATE tracking
SET state = $1,
    updated_at = now()
WHERE id = $2
RETURNING id, media_item_id, quality_profile_id, state, scope, upgrade_behavior, schedule_strategy, autonomy_backfill, autonomy_ongoing, created_at, updated_at
`

type SetTrackingStateParams struct {
	State string      `json:"state"`
	ID    pgtype.UUID `json:"id"`
}

func (q *Queries) SetTrackingState(ctx context.Context, arg SetTrackingStateParams) (Tracking, error) {
	row := q.db.QueryRow(ctx, setTrackingState, arg.State, arg.ID)
	var i Tracking
	err := row.Scan(
		&i.ID,
		&i.MediaItemID,
		&i.QualityProfileID,
		&i.State,
		&i.Scope,
		&i.UpgradeBehavior,
		&i.ScheduleStrategy,
		&i.AutonomyBackfill,
		&i.AutonomyOngoing,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
